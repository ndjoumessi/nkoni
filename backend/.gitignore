node_modules
dist
# Keep environment variables out of version control
.env

/src/generated/prisma

# Miroir Blob local (dette GA D2) — sortie de `npm run mirror:blobs --apply`, poussée vers R2 par
# le workflow. Fichiers téléversés (documents/photos), JAMAIS commités.
blob-mirror/
